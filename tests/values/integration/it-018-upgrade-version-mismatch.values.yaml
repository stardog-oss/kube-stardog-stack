# Integration scenario file ID: IT-018
# Covers test: 18
# Purpose: negative test where upgrade approval targetVersion mismatches image.tag.
# Requires: `stardog-license` secret only if you continue past the expected upgrade validation failure.
# Run as-is: yes
# Limitations:
# - This file is expected to fail at render or install time.
# - It is only useful for validating the explicit upgrade mismatch error path.
# Update before use:
# - No update required unless you want to test a different explicit version mismatch.

global:
  stardog:
    enabled: true
  launchpad:
    enabled: false
  voicebox:
    enabled: false
  bi:
    enabled: false
  gateway:
    enabled: false

stardog:
  image:
    tag: 9.2.1
  upgrade:
    approval:
      targetVersion: 9.2.2
  ingress:
    enabled: false
  route:
    enabled: false
  gateway:
    enabled: false
